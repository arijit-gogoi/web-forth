// @web-forth/cli — headless node REPL over @web-forth/engine.
// Scaffold placeholder; real REPL lands via SPEC.md §T.12.
import { ENGINE } from '@web-forth/engine'

console.log(`${ENGINE} cli — scaffold`)
