// @web-forth/client — Foldkit + CodeMirror 6 SPA.
// Scaffold placeholder; Vm service, app skeleton, and panes land via SPEC.md §T.13-18.
import { ENGINE } from '@web-forth/engine'

export const CLIENT = `${ENGINE} client`
