node_modules/
dist/
*.log
.DS_Store

# NOTE: packages/engine/src/prelude.generated.ts is committed (not ignored): the cli
# and client import it, and a consumer's typecheck/build does not fire engine's
# pretest hook. It is regenerated by scripts/gen-prelude.mjs (gen:prelude / pretest);
# staleness shows up as a git diff.

# scratch / handoff space (not tracked)
.handoff/

# NOTE: repos/ is intentionally NOT ignored — it holds vendored subtree source.
